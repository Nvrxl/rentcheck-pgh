# AI usage log

SteelHacks allows AI help with writing code as long as it is credited. This file is our record.
The finished app itself uses **no language models** (No Wrapper track requirement): report text is
built from templates and all logic is ordinary code.

## Tools used
- Claude (Anthropic): brainstorming, competition research, project scaffolding, code generation, debugging.

## Rules for ourselves
- Add a row when AI writes or substantially changes something.
- We read and understand everything we ship. If we can't explain it to a judge, we simplify it.

## Log

Times are approximate (we write them down after the fact); the Git history has the exact commit times.

| When (ET) | Who | Tool | What we asked | What we used / changed |
|---|---|---|---|---|
| Sat Sep 19, earlier in the afternoon (approx.) | Michael | Claude | Idea brainstorming; researched competition; picked "Look Before You Rent" for Pittsburgh | Chose the idea and tracks |
| Sat Sep 19, earlier in the afternoon (approx.) | Michael | Claude | Generate the starter project (Javalin backend, city data client, sample frontend, docs, task split) | Whole starter: `pom.xml`, `src/main/**`, `README.md`, `CLAUDE.md`, `docs/*`. Not yet compiled by Claude (no Maven access in its sandbox); we test and fix. |
| Sat Sep 19, earlier in the afternoon (approx.) | Michael | Claude | Fix the app to use the city dataset's real column names (pasted /api/debug/fields output); better address matching | `Fields.java`, new `AddressNormalizer.java` (tested in isolation), `WprdcClient.java` (address-only search, `/api/debug/distinct`), `ReportService.java` (only count real violations), `index.html` header |
| Sat Sep 19, earlier in the afternoon (approx.) | Michael | Claude | Pasted real investigation_outcome counts; asked for exact rule for what counts as a violation | `ReportService.java`: allow-list of violation outcomes, rule for rows with no outcome |
| Sat Sep 19, earlier in the afternoon (approx.) | Michael | Claude | Pasted real status and case_file_type counts; asked to use them | New `Categories.java` (keyword buckets, tested against the real case types), `ReportService.java`: exact open statuses, safety-focused risk rating, category buckets |
| Sat Sep 19, earlier in the afternoon (approx.) | Michael | Claude | Pasted raw city rows for 1231 Lakewood St; asked to fix over-counting | `ReportService.java`: group rows by case number so one case = one violation; `Models.java`: new fields caseId/resolvedDate/codeSection; tested against the real rows |
| Sat Sep 19, ~3:30 PM | Michael | Claude | Wanted test addresses that show MEDIUM/HIGH risk ratings | `WprdcClient.java` + `App.java`: `/api/debug/hotspots` helper. The first version used the city's SQL endpoint and got HTTP 403, so Claude rewrote it to use the plain search and count cases in Java |
| Sat Sep 19, ~3:55 PM | Michael | Claude | Tested real addresses; 3619 Forbes Ave (a restaurant with a paperwork case and stale 2020 court cases) was rated HIGH, which looked wrong | `ReportService.java`: points-based rating (recent open = 3, old open = 1, resolved recent = 1 capped at 4; HIGH at 8, MEDIUM at 3), unit-tested on 12 cases. New `docs/HOW_WE_SCORE.md` |
| Sat Sep 19, ~4:30 PM | Michael | Claude | Replace the draft "How we score" text; make the page dark by default with a Light/Dark button | `index.html` (score panel, button), `style.css` (dark default, light theme), new `theme.js` (tested in a headless browser) |
| Sat Sep 19, ~4:45 PM | Michael | Claude | Neighborhood ranking and "near you" rentals links (crime ranking not possible: the city's crime feed stopped in Nov 2023) | New `NeighborhoodService.java` (counting, ranking, nearest neighborhood, unit-tested on made-up data), `WprdcClient.java` (open-cases and population fetchers), `App.java` (`/api/neighborhoods`, `/api/neighborhoods/near`, `/api/debug/population`), `docs/API_CONTRACT.md` |
| Sat Sep 19, ~5:15 PM | Michael | Claude | Pasted the real neighborhood ranking and population table; asked for improvements | `NeighborhoodService.java`: matches the three differently spelled neighborhood names, ranks per 1,000 residents, flags the duplicated Shadyside/Squirrel Hill South population. New `Questions.java` ("questions to ask before you sign", from templates), `Models.java` (new `questions` field), `WprdcClient.java` + `App.java` (`/api/debug/peek`), `docs/API_CONTRACT.md`. All unit-tested on made-up data |
| Sat Sep 19, ~5:45 PM | Michael | Claude | Pasted the county property-assessment columns from /api/debug/peek; asked to use them | New `PropertyService.java` (building type, year built, rooms, owner type, homestead flag; owner NAMES are excluded by county ordinance), `WprdcClient.java` (parcel lookup by house number + street), `App.java` (`/api/debug/property`, plus `/api/health` now reports when the server started). Unit-tested against the real row shape |
| Sat Sep 19, ~6:15 PM | Michael | Claude | Pasted real county results: 3619 Forbes Ave came back as class GOVERNMENT (Pitt-owned) and 219 4th Ave as COMMERCIAL | `PropertyService.java`: plain-English building type (the county's "GOVERNMENT" class would mislead a renter) and a context warning when an address is not housing; `Models.java` + `ReportService.java`: reports now carry an "About this building" block, wrapped so a county failure can't break a report; `docs/API_CONTRACT.md` |
| Sat Sep 19, ~6:40 PM | Michael | Claude | Reviewed Connor's new tabs; he proposed a `neighborhood` field in docs/API_CONTRACT.md and had already built the UI for it | `Models.java` + `ReportService.java`: reports now say which neighborhood an address is in and its rank (name taken from the city's own column, most common value; never waits for the ranking to finish loading); `App.java` wiring; `docs/API_CONTRACT.md` (proposal marked as built) |
| Sat Sep 19, ~5:15 PM | Michael | Claude | Asked to deploy the app so judges can open it from a real web address | `pom.xml`: maven-shade-plugin so `mvn package` builds one runnable `target/rentcheck.jar`; new `Dockerfile` (build with Maven, run on a Java-only image) and `.dockerignore`. Works for both Render and DigitalOcean App Platform |
| Sat Sep 19, ~8:10 PM | Michael | Claude | "No records found" looks the same for a clean address and a typo; asked for a "did you mean" | New `Suggestions.java` (Levenshtein edit distance + house-number weighting, no LLM), `WprdcClient.java` street search, `ReportService.java` + `Models.java` return `suggestions` when nothing matched. 20 unit tests |
| Sat Sep 19, ~8:30 PM | Michael | Claude | We only linked Pitt's housing office, and we wanted a "how far from my campus" filter | New `Schools.java` (Pitt, CMU, Duquesne: official housing offices + campus coordinates from each university's Wikipedia infobox, travel modes with default radii), `NeighborhoodService.java` distance-from-a-point, `App.java` `/api/schools` and `?school=&mode=` filtering, `docs/API_CONTRACT.md`. 25 unit tests |
| Sat Sep 19, ~9 PM | Michael | Claude | Asked what other public data would make the report more useful; researched, then built the two best | New `Condemned.java` (city's daily condemned/unfit-for-occupancy list - the most serious thing we can tell a renter; we deliberately do NOT expose the owner names the dataset contains) and `Permits.java` (building permits since June 2019 with project values, deduplicated by permit id, shown next to the violation count so a renter can see whether work is actually being done). `WprdcClient.java` parcel-id lookups, `Models.java`, `ReportService.java`, `docs/API_CONTRACT.md`. 32 unit tests built from the real rows, which caught that the live data uses one combined "Condemned/Dead End" label so we must not claim the owner is missing |
| Sat Sep 19, ~9:40 PM | Michael | Claude | Michael corrected us: the city's 311 data is NOT dead, only the old archive is. Asked to use the current source | Fixed the wrong line in `CLAUDE.md` (shared file, Connor told). New `Requests311.java`: 311 requests for the address's neighbourhood. Column names are DISCOVERED AT RUN TIME rather than hardcoded, since we had not seen this dataset's columns - `/api/debug/311` shows what it picked, and anything it cannot find goes unused instead of being guessed. Looked up by neighbourhood, not radius, because the city withholds some complaint locations. Also new `dataAsOf` block: every dataset with what it covers and the newest record seen. 24 unit tests |
| | | | | |
